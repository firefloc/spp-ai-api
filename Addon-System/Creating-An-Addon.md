# Creating an Addon

> **Status**: Placeholder — this guide will be completed with Brique 7 when the addon system is implemented.

## Prerequisites

- spp-lib as a dependency (Maven local or repository)
- Fabric mod development setup
- Familiarity with [PersonaModule](../API-Reference/PersonaModule) interface

## Quick Start (Future)

1. Create a new Fabric mod project
2. Add spp-lib dependency to `build.gradle`
3. Implement `SppAddonEntrypoint`
4. Register custom modules in `onLoad()`
5. Declare the `spp-addon` entrypoint in `fabric.mod.json`

Detailed instructions will be added when the addon system is implemented.
